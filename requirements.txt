# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data / ORM
SQLAlchemy==2.0.30

# Config / validation
pydantic==2.7.4
pydantic-settings==2.3.4

# LLM + embeddings
openai==1.35.7
# Pin httpx: openai 1.35.7 breaks on httpx >=0.28 (removed the `proxies` arg).
httpx==0.27.0

# Database driver + vector storage (Postgres + pgvector)
psycopg[binary]==3.2.9
pgvector==0.3.6

# Auth: JWT + password hashing + form/email parsing
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
# Pin bcrypt: passlib 1.7.4 errors on bcrypt >=4.1 (removed __about__).
bcrypt==4.0.1
python-multipart==0.0.9
email-validator==2.1.1

# Retry / backoff
tenacity==8.4.1

# Eval metrics
pandas==2.2.2

# Misc (.env loading for pydantic-settings)
python-dotenv==1.0.1
